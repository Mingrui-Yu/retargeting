# @package solver
name: scipy_slsqp
params:
  # SciPy SLSQP `ftol` controls multiple convergence checks, not just objective-value change.
  ftol: 0.00001
  # Negative or zero disables the per-frame wall-clock time limit.
  maxtime: -1
